---
interface Props {
  id: string;
  title?: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="scroll-mt-20 py-12">
  {title && (
    <h2 class="mb-6 text-xl font-semibold tracking-tight text-fg dark:text-fg-dark">
      {title}
    </h2>
  )}
  <slot />
</section>
